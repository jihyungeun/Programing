1 input 값 -> 확인 -> 맞으면 -> 비밀번호 입력 가능
		    틀리면 -> 쓰잘대기 없는 내용

2 input 값 -> 확인 -> 맞으면 -> 접근 가능
                        -> 틀리면 -> 횟수 카운트 -> 숫자안에 하면 -> 접근 가능
			        특정 횟수 범위 안에 있으면 -> 경고
					숫자벗어 나면 -> 종료

접근 했다면 -> 특정 명령어 입력 전까지 프로그램 유지
	   -> 특정 명령어 입력 시 저장 후 프로그램 종료